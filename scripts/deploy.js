const { ethers, run, network } = require("hardhat");

async function main() {}

async function verify() {}

main()
  .then(() => process.exit(0))
  .catch((err) => {
    console.log(err);
    process.exit(1);
  });
å;
